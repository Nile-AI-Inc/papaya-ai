# Gemini — for the gemini-* and error-handling scripts (https://aistudio.google.com/apikey)
GEMINI_API_KEY=

# Bedrock — for agent.ts
AWS_PROFILE=
AWS_REGION=us-east-1

# Papaya — optional: each script sets apiKey inline ("XXX"). Use this only if you
# switch the scripts to apiKey: process.env.PAPAYA_API_KEY
PAPAYA_API_KEY=
